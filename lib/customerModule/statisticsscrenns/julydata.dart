import 'package:flutter/material.dart';

class Julydata extends StatelessWidget {
  const Julydata({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    backgroundColor: Colors.white,
    );
  }
}