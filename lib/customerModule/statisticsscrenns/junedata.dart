import 'package:flutter/material.dart';

class Junedata extends StatelessWidget {
  const Junedata({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    backgroundColor: Colors.white,
    );
  }
}