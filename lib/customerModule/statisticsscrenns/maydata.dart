import 'package:flutter/material.dart';

class Maydata extends StatelessWidget {
  const Maydata({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    backgroundColor: Colors.white,
    );
  }
}